export default class BlindsStructureItem {
    constructor(props) {
        this.user_level = props.user_level;
        this.times = props.times;
        this.blind_label = props.blind_label;
    }
}
