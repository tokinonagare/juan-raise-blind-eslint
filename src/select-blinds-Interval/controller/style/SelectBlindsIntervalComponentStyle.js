import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    homeContainer: {
        flex: 1,
        justifyContent: 'space-between',
        marginTop: 15,
    },
    navigationCentre: {
        alignItems: 'center',
    },
});
